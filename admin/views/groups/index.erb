<div class="card">
    <div class="card-title">
        <h4 class="underline">团购列表</h4>
    </div>
    <div class="card-toolbar">
        <div class="btn-group btn-group-sm">
            <button class="btn btn-default" type="button" data-toggle="modal" data-target="#searchModal">
                <i class="fa fa-search"></i> 搜索
            </button>
            <a href="<%=url(:groups, :select_sku, :from => 'groups')%>" class="btn btn-default"><i class="fa fa-plus"></i>创建团购</a>
        </div>
    </div>
    <!-- 表格主体内容 -->
    <table class="table table-condensed table-striped table-bordered-y">
        <thead>
        <tr>
            <th style="width: 10%;">拼团标题</th>
            <th style="width: 13%;">拼团描述</th>
            <th style="width: 15%;">商品</th>
            <th style="width: 15%;">开始结束时间</th>
            <th style="width: 10%;">团购价(原价)</th>
            <th style="width: 8%;">人数限制</th>
            <th style="width: 7%;">销售数</th>
            <th style="width: 10%;">状态</th>
            <th style="width: 10%;"></th>
        </tr>
        </thead>
        <tbody>
        <% @groups.each do |group| %>
            <tr>
                <td><%=group.title%></td>
                <td><%=group.detail%></td>
                <td><%=group.t_sku.title%></td>
                <td><%=group.begin_time.try{|bt| bt.strftime('%Y%m%d %H:%M')}%>~<br/><%=group.end_time.try{|et| et.strftime('%Y%m%d %H:%M')}%><br/>
                  <% if group.begin_time > Time.now %>
                    <span style="color: #3498DB;">(未开始)</span>
                  <% elsif group.end_time < Time.now %>
                    <span style="color: #3498DB;">(已结束)</span>
                  <% end %>
                </td>
                <td>￥<%=group.group_price%>(￥<%=group.t_sku.price%>)</td>
                <td><%=group.min_num%>~<%=group.max_num%></td>
                <td><%=group.group_buyers.purchased.count%></td>
                <td><%=Group::STATUS[group.status.to_s]%></td>
                <td>
                  <div class="dropdown">
                    <button type="button" class="btn btn-default btn-xs dropdown-toggle" data-toggle="dropdown">
                        <i class="fa fa-gear"></i> <i class="fa fa-angle-down"></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-right" role="menu">
                      <% if group.status == 1%>
                        <li role="presentation"><a href="<%=url(:groups, :change_status, :id => group.id, :status => '0')%>">下架</a></li>
                        <li role="presentation"><a href="<%=url(:groups, :change_status, :id => group.id, :status => '2')%>">成团</a></li>
                        <li role="presentation"><a href="<%=url(:groups, :change_status, :id => group.id, :status => '3')%>">未成团</a></li>
                        <li role="presentation"><a href="<%=url(:groups, :edit, :id => group.id)%>">修改拼团信息</a></li>
                        <% elsif group.status == 0%>
                        <li role="presentation"><a href="<%=url(:groups, :change_status, :id => group.id, :status => '1')%>">上架</a></li>
                        <% end %>

                        <% if group.group_buyers.count > 0%>
                        <li role="presentation"><a href="<%=url(:groups, :show, :id => group.id)%>">购买记录</a></li>
                      <% end %>
                    </ul>
                  </div>
                </td>
            </tr>
            <% end %>
        </tbody>
    </table>
    <div class="pagination">
      <%= will_paginate @groups %>
    </div>
</div>
<div class="modal modal-custom fade" id="searchModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title underline">搜索模态框</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="row">
                        <div class="col-xs-6 form-group">
                            <label>文本</label>
                            <%= text_field_tag :text, :value => nil, :class => 'form-control' %>
                        </div>
                        <div class="col-xs-6 form-group">
                            <label>选择栏</label>
                            <%= select_tag :select, :options => %w[option1 option2], :class => 'form-control' %>
                        </div>
                    </div>

                    <div align="right">
                        <button type="button" class="btn btn-back" data-dismiss="modal">关闭</button>
                        <button type="button" class="btn btn-submit">提交</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>